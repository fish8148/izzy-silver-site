// The widths (px) `npm run prep:photos` resizes every photo to. Shared by the script and the site, so they agree.
// 800 covers a photo card on a high-density screen, 1600 the big one in the carousel.
export const PHOTO_WIDTHS = [800, 1600];
